#include <stdio.h>
#include <unistd.h>
#include <pthread.h>
#include <stdlib.h>

#define MAX_THREAD 10

typedef struct {
        int id;
} parm;

void *hello(void *arg)
{
        
        return (NULL);
}

int main (int argc, char ** argv) {
	
  exit(0);

}